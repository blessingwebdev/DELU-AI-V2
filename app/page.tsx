"use client";
import {useEffect,useState} from 'react';
export default function Companies(){const [d,setD]=useState<any>();useEffect(()=>{fetch('/api/companies').then(r=>r.json()).then(setD)},[]);return <main className="mx-auto max-w-7xl px-6 py-10"><h1 className="text-3xl font-bold">NGX Company Registry</h1><p className="mt-2 opacity-70">Official-source discovery is used to keep the registry current.</p><div className="mt-6 rounded-2xl border bg-white p-6"><b>{d?.count??'…'} companies discovered</b><div className="mt-5 grid gap-2 sm:grid-cols-2 lg:grid-cols-4">{d?.companies?.map((c:any)=><div key={c.symbol} className="rounded-xl border p-3"><b>{c.symbol}</b><div className="text-xs opacity-60">{c.name}</div></div>)}</div></div></main>}
