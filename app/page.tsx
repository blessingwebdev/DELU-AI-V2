"use client";
import {useEffect,useState} from 'react';
export default function Dashboard(){const [d,setD]=useState<any>();useEffect(()=>{fetch('/api/market').then(r=>r.json()).then(setD)},[]);return <main className="mx-auto max-w-7xl px-6 py-10"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm opacity-60">DELU AI V2</p><h1 className="text-3xl font-bold">Market Dashboard</h1><p className="mt-2 opacity-70">Clear data. Honest confidence. No invented prices.</p></div><a href="/ask-delu" className="rounded-xl px-5 py-3 text-white" style={{background:'var(--wine)'}}>Ask DELU</a></div><div className="mt-8 grid gap-4 md:grid-cols-4"><Card t="Market" v={d?.marketStatus||'…'}/><Card t="Verified" v={String(d?.verifications?.filter((x:any)=>['VERY_HIGH','HIGH','CONFIRMED'].includes(x.confidence)).length??'…')}/><Card t="Sources OK" v={String(d?.sourceResults?.filter((x:any)=>x.ok).length??'…')}/><Card t="Synced" v={d?.syncedAt?new Date(d.syncedAt).toLocaleTimeString():'…'}/></div><section className="mt-8 rounded-2xl border bg-white p-6"><h2 className="text-xl font-bold">Live verification</h2><div className="mt-4 grid gap-3">{d?.verifications?.slice(0,30).map((x:any)=><div key={x.symbol} className="flex items-center justify-between rounded-xl border p-4"><div><b>{x.symbol}</b><div className="text-xs opacity-60">{x.reason}</div></div><span className="rounded-full border px-3 py-1 text-xs">{x.confidence}{x.price!==null?` · ₦${x.price}`:''}</span></div>)}</div></section></main>}
function Card({t,v}:{t:string,v:string}){return <div className="rounded-2xl border bg-white p-5"><div className="text-sm opacity-60">{t}</div><div className="mt-2 text-2xl font-bold">{v}</div></div>}
